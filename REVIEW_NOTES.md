# Review Notes

The extension has a single purpose: help YouTube Studio users draft replies to comments.

The extension uses user-provided API keys for OpenAI, Gemini, or Anthropic. It does not include API keys, does not use a developer-operated backend, and does not execute remote code.

The extension inserts reply text as a draft only. It does not click the final YouTube submit/reply button.
